# LAB4
Missing Values Handling
I used median imputation for numeric columns because it is less affected by outliers. For categorical columns, I filled missing values with the mode. This keeps the dataset size while handling missing data properly.
